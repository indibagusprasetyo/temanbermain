// looping nest / nested loop
// loop dalam loop

for (let i = 0; i <= 5 ; i++) {
    console.log("- FIRST LEVEL LOOP");
    for (let x = 0; x <= 8; x++) {
        console.log("--SECOND LEVEL LOOP")
    }
}