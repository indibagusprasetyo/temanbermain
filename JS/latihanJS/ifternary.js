// if ternary atau conditional expression
// condition ? true expression : false experession

const anggota = false;
const diskon = anggota ? 0.1 : 0; // if anggota then diskon 10%, else 0%
console.log(`Anda mendapatkan diskon sebesar ${diskon * 100}%`)